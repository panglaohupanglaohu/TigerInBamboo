import { profileById } from "../terrainProfilesV8.js";
export function createHighlandTerrainProfile({ landmark }) { return { id: "highland-citadel", recipe: profileById("highland-citadel"), landmark }; }
