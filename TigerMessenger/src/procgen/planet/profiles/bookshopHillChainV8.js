import { profileById } from "../terrainProfilesV8.js";
export function createBookshopHillChainProfile({ landmark }) { return { id: "bookshop-town", recipe: profileById("bookshop-hill-chain"), landmark }; }
