最新模型接入后的实际总览绘制为6332→517（植被变体也有变化，不能与旧版6974直接混比）；帧时间仍约16.7ms。正式源哈希及最新图像差异见validation.json，下方旧计数为换树前历史。

## 最新修订：按用户目标图在 Blender 修改植物种类与模型

上一版直接复用松树不足以表达目标。本次实际 Blender 建模形成 `reference-plants-v1.blend`：原松树完整置于隐藏归档集合；阔叶树复用并拉长原树干网格、重做分枝及9团树冠，柏树采用细长连续冠层轮廓。两个 GLB 位于 project/assets/broadleaf-reference-v1.glb 与 cypress-reference-v1.glb；Blender真实渲染 `plants-blender.png`，制作脚本 tools/build_reference_plants.py。

Godot候选4处近景改为3阔叶＋1柏树；12个原山坡树根保留身份与贴地位置，隐藏旧冠层并接9阔叶＋3柏树，保留回退源。模型整体仍为low-poly，尚未逐树验证角色净空，不能将新模型称为最终美术达标。下方“原树直接复用”是此前第三轮历史，本修订覆盖第四轮当前可见树型。

# 圣城蓝夜第三、四轮接续

仅写本独立候选目录，没有改主线程生产文件、存档或活动编辑器。四轮迭代产物已形成，但目标图美术、完整通行与部署验收并未全部完成。

## 第三轮：植物、原街灯与水面

- 复用 `godot/assets/originals/lamp.glb` 和 `lowPolyTree.glb`，哈希校验一致。6盏原街灯按实际低层窗前地面射线落脚，灯源对齐原灯球(0.48,1.95,0)，保留原造型，候选比例1.4。4处小树使用原资产，比例0.85；未引入外部素材包。
- Blender真实后台操作 `round-03/terrain-plants-v3.blend`：625个树冠/草实例中620个按原山面与新山面高度差平移，保留原XY和几何，最大位移约24.984。5个无交点未移动；不能称全部植被贴地通过。实际报告 `round-03/plant-grounding.json`。本次是独立后台Blender，未谎称操作前台MCP。
- 原城市GLB中的12个山坡植被根在Godot按新山面重新贴地，只移动根节点，保留其冠层局部变换。初次匹配误含canopy后代，已经收紧为完整数字根名；正式第四轮报告仅12个根。
- 复用原 highland-waterfront-water 的曲面水几何与岸线，未新增平面水池。原WFC建筑GLB与第一轮哈希一致。

## 第四轮：遮挡、反射、静态整理与实景检查

- 独立候选采用Godot Forward+/Metal，所有6盏局部灯开启阴影，配合原方向光和SSAO。输出岸边同镜头阴影开/关图，墙面明暗有实际差异；这不是逐条巷道零漏光或通行认证。
- 水使用不透明波纹法线材质、SSR及实际场景ReflectionProbe；反射开/关和两个波纹时刻均有图。探针是空间近似，动态场景需刷新；SSR无法补齐屏幕外遮挡。波纹改变法线不改变水岸几何。目标图的金红长倒影仍未达到，原水岸与台地衔接在近景仍显生硬。
- 1719个静态不透明叶网格按材质/阴影模式合成586批，原节点保留可回退；水和透明物不参与。总览绘制约6974→1196；合批降低剔除精度，渲染图元增加，不能只拿调用下降当全部性能通过。30帧短样本接近16.7ms，与刷新率上限相近，不声称速度提升倍数。
- 同镜头合批前后平均RGB绝对差约[0.009,0.016,0.015]/255，不是严格逐像素零差。多视角和效果差异见 `validation.json`，实际帧计数 `godot-report.json`。
- WFC重验：978格、2436条有向邻接、确定性、导出选型匹配及不兼容反例拒绝均通过。本轮未发现需修改求解器的反例，没有为了计工作量再改权重。角模块接缝与真实导航仍待单独几何验证。

## 使用与复验

`index.html`可切总览、水岸、侧面、阴影/反射诊断及波纹时刻。Godot打开 `project/project.godot`，默认蓝夜；1总览、2水岸、3侧面、L街灯、空格日夜。渲染复现：Godot --path 此目录/project -- --capture。WFC复验：项目根下运行 `rtk proxy node artifacts/citadel-blue-night/round-04/tools/check_wfc.mjs`。

引擎退出仍报告7个Texture RID未释放，释放探针后仍可复现；记录为候选生命周期问题，尚未修复，不能称无警告。纹理捕获与文件输出成功不等于该问题不存在。

## 接续问题

1. 修原曲面水岸与地基衔接、进一步匹配岸灯金红反光；不要改平水面迎合单镜头。
2. 5个植被射线漏项及大冠层与陡壁关系；四个新增树木口袋仍需实际角色路径验证。
3. 按原WFC街道/门洞定义做真实可达性、拱廊净空、屋顶接缝检查，出现具体反例才改对应约束。
4. 反射探针释放告警、静态批次空间分块剔除、长时间多视角性能。
5. 目标图峡谷折面与植被密度仍不足；本轮没有声称完美复刻或全城可玩交付。

技术依据：[Godot环境与反射说明](https://docs.godotengine.org/en/latest/tutorials/3d/environment_and_post_processing.html)。本次使用 threejs-aaa-graphics-builder 的 authoring-recipes / technical-art；没有启用子代理。
