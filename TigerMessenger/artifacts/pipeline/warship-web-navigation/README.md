## v6 接入后复验（2026-09-10）

最新 `2026-09-09T22-59-27.155Z/report.json`：10 项通过，源文件前后哈希一致，0 pageerror。真实两船均校验 v6 GLB SHA。去返程前向/速度点积最低0.9999999999999997。去程截图仍被原建筑遮挡，返程原贴岸与终点3.99/3.96米重摆仍在，未宣称解决。

# Web 传统战船航向复验

最终报告：[2026-09-09T22-02-39.227Z/report.json](2026-09-09T22-02-39.227Z/report.json)。10 项通过，实际页面无错误，运行源哈希前后一致。

## 模型方向与实际版本

原 `src/assets/harbor.js:createFisherBoat` 为 Web 当前实际模型，+X 是 `hull-prow` 青铜撞角和原双眼所在船头，+Y 为甲板上方，-X 卷曲端/货物区为船尾。Blender v5/v6 也应遵守此契约；本次没有把 v5/v6 静态模型接入游戏，没有修改船体或桨手几何。

## 已修内容

- `src/world/warshipNavigation.js`：船用正交右手系必须满足 X×Y=Z。旧 `surfaceBasis` 的 `up×forward` 搭配 `makeBasis(forward,up,right)` 产生 determinant=-1 的反射矩阵，交给四元数会产生错误朝向和非单位四元数。只替换船只姿态，共用士兵坐标逻辑未在本次扩改。
- `src/world/saihojiPhalanx.js`：去程、返程、红方增援、蓝方左右增援及泊船均改用船用方向函数。去返程使用当前航段切向，交汇处静止保留朝向，不用跨越停靠段的未来采样点提前转头。
- 蓝方增援原位置带左右偏移，旧朝向目标没有对应偏移；现采用该实际侧移曲线的切向。
- 原航路端点、34 秒去返程进度、增援航时、桨手与船桨动作均保留，没有反转航线来配模型。

## 复验范围

命令：`rtk proxy node tools/pipeline/test_warship_navigation_web.mjs`。

实际 8931 页面恢复第二章，使用原战斗 update 推进两艘船完整去程。之后通过原 `whaleReturned()` 故事回调诊断触发返程；没有宣称自然完成全部鲸战。逐移动帧测实际船四元数、+X 与位移方向、真实撞角位置与速度方向。去程 3754 个船帧、返程 3756 个船帧，最小前向点积分别 1.000000000000 / 1.000000000000。单位四元数及甲板径向正立均通过。

另做 12 种球面旋转、出/返/红援/蓝左援/蓝右援合计 60,060 次路线数学采样，含停靠段与端点。红蓝援军这部分为独立路线 fixture 与调用点审阅，不是完整攻城实战验证。原反射矩阵朝向错误在同组采样中复现。

## 明确未验收

- 第一轮报告 `2026-09-09T22-00-35.002Z/report.json` 保留。它把最终 `beginSiege` 原泊位重摆计为航行，最小点积降到 .453；最终报告单列 `dockingTransitions`，不以排除此帧宣称泊位转换连续。
- 原返程终点泊位位置重摆仍存在，实际位移见最终报告。没有修复或接受其跳变。
- 原船沿固定半径 R+.18 行驶，去程初段被圣城建筑/台地遮挡，返程起点船体贴入苔庭板面。两张原场景实图如实保留；本次通过只表示航向，不代表航线/碰地/水面/登陆净空通过。
- 未验证 25 人登船调度、26 桨叶/手柄净空，未接 Godot，未改变本体美术。

模型新两轮见 [独立 Blender 船头对照](../warship-prow-review/index.html)。
