# 概念图 → Blender MCP → 游戏截图迭代

## 2026-09-08 续接：实际环境的视线与墨线

Web 书店招牌、建筑和门前通路增加局部林带避让，实际过滤 3 棵遮挡树，保留其他走廊树；不修改树的设计或世界其他区域。Godot v3 书店回接原 Web 暖墨反向壳、压力变化与少量飞白，26 部件合批仅增加 1 次绘制。已查看真实游戏画面及 Godot 同镜头同灯光开/关对照，并通过移动/碰撞/交信验证。证据：`artifacts/bookshop-refinement/`。

Godot 表面仍为标准材质，并未完成 Web 两阶明暗的移植；原完整书店镇仍待迁移。本批源 Blender/GLB 不变，下方“墨线尚需适配”属于此前历史状态。

## 2026-09-08 最新：第三轮书店已接入游戏环境

用户指出“图片看到了，但 Blender 模型没进环境”。实际核对发现已有 `bookshop-art-v3.blend` 和 `godot/assets/art-pilots/bookshop-art-v3.glb`，旧记录只写到 v1；v3 此前仅用于检视场景。

- Web 默认书店现在从 v3 保存副本导出并使用实际评估后的建筑网格、节点变换和窗框；保留运行时墨线、可编辑招牌、地形土坡、任务与碰撞引用。窗框 50 件合并成一次绘制；书店场景花丛采用 v3 的两侧布局。
- Godot 默认 `scenes/main.tscn` 的书店地点直接使用 v3 GLB，包含窗框、绣球与石径；庭园外缘适应球面，并补对应碰撞、入口朝向及取景。
- Godot 已实际通过出生点→书店→门前步行、墙体阻挡、R 键交信；Web 已通过新版来源、10 扇窗无遮蔽、招牌编辑、地形变体、默认世界真实输入与接信检查。最终截图与复查报告见 `artifacts/bookshop-integration/`。
- 原件、v1/v2/v3 保存文件和前台未保存编辑保留。此次读取前台 Blender 确认状态；实际导出在独立后台 Blender 中读取保存的 v3，不保存回源文件。
- 这是书店回接，不等于整个原作世界迁移或美术最终验收；Godot 周围仍有原先实验内容，墨线尚需适配。

下方为历史制作记录，不再将“尚未替换默认书店”作为最新状态。

2026-09-08，按用户给出的流程执行。本文件记录实际执行，不把社交媒体对具体模型、推理设置或“无限接近”的描述当成验证结果。

## 首个样板：原作书店

- 目标图：`assets/concepts/bookshop-target-v1.png`，使用内置 imagegen；输入参考为 `assets/models/optimized/verification/bookshop-web-before.png`。
- 提示词约束：保留高砖红平顶主体、两侧凸窗、白色三角门廊、细柱与左前黄黑 HARD TO FIND / BOOKSHOP 招牌，不添加新楼层或改成通用城堡；增强克制倒角、抹灰质感、接地台阶、蓝紫绣球与石板路径，暖日光/冷天光，保持 low poly、球面世界和可走入口。
- 概念图是候选美术目标，不是已完成的游戏截图。原代码已有而被遮住的玻璃窗必须修复，不能因目标图也没显示窗户而继续保留缺陷。
- 已通过真实 MCP execute_blender_code 在前台 Blender 中创建独立 `Bookshop Art V1` 场景，旧虎场景仍在内存；不覆盖当前原文件。
- 修改记录：`artifacts/bookshop-art-v1/changes.json`；执行请求：同目录 load.json / edit.json，可供接手者审查，但勿重复执行创建场景请求。
- 模型副本：`assets/models/optimized/bookshop-art-v1.blend`。已调整窗户、门洞和小倒角；尚未替换 Godot 或 Web 默认书店。

## 每轮必须交付

1. 原作与参考来源、目标概念图、必须保留的设计元素。
2. Blender 实際修改与可编辑副本，摄像机/光照配置及渲染。
3. 同镜头的游戏截图；逐项判断轮廓、比例、配色、表面、光照、接地/穿模。
4. 记录仍有差距的项目与下一轮动作。概念图和游戏不要求逐像素相同；无损拓扑整理才使用零像素变化测试。
5. 保留可回退版本，验证交互、动画、碰撞与性能后才标“游戏回接完成”。

当前下一步：查看第一轮 Blender 渲染，修正窗户定位和材质问题；补齐概念中的原作绣球、地面与照明，将样板导入独立 Godot 场景进行对照。
