## 2026-09-11：MCP 已恢复，圣城地形候选实际迭代

Blender 原生 MCP 已能读取和执行。原窗口实际为 shoreline-corrected.blend，含未保存内容，已先保留 baseline-preserved.blend；未覆盖原文件，也未另开 Godot。

证据目录 artifacts/citadel-target-iterations/：基线及 round-01、02、03 各有 .blend 和实渲染 .png。第一轮细分/蓝灰分面，第二轮处理归档峡谷重叠与基础侧面，第三轮缓解尖碎坡面。相机/光照固定。round-01/02 的保护顶点检查为0丢失。第三轮仅对半径>37、高度>9的外围顶点平滑。

质量判断：内壁长三角折痕仍明显，城市/山体比例与认可图差距大；本批不发布。三次地形试验不等于原计划的地形、台面雕塑、灯光三轮整体完成。台面建筑/雕塑尚未完成，8931/Godot尚未接入这批候选。对照城堡来自归档original.glb，不包含最新Web旋梯/庭院修复，不得整城回灌覆盖。WFC建筑未修改。

下一动作：修复内壁拓扑、调整山城比例；换用最新运行时WFC对照，继续前景台面/雕塑；通过后选择性接入Web/Godot与碰撞验证。查看 artifacts/citadel-target-iterations/index.html。

## 2026-09-11 Blender 恢复诊断

现有 PID33560，状态 S，未暂停；1秒调用栈采样显示主线程仍处理界面事件，没有证据支持进程崩溃。原实例占用约761MB物理内存；未另开前台实例。9876端口可连接，前台激活后只读 execute_code 仍在5秒超时；停止重复重试。截图中存在打开文件对话框，但尚未确认所属应用，不能断定它是根因。读取 Blender 窗口信息时系统明确拒绝 osascript 辅助访问（-1728），无法安全自动操作插件界面。

需要用户在已有 Blender 窗口中退出未完成的文件选择/其他弹窗（如存在），然后在 MCP 面板停止并重新启动连接；无需重装。未确认当前文件未保存状态，不能强制退出或覆盖书店。恢复后首先只读核对当前文件，再建立独立圣城副本和基线渲染。本轮未完成任何建模迭代。

# 高山圣城：WFC + Blender 目标图迭代

2026-09-11，用户明确要求。目标图 assets/concepts/citadel/citadel-battle-target-v1.png 已获认可。

## 分工

- 城堡与城镇建筑：保留原 WFC 求解、邻接和庭院规则，以及增量编辑能力。Blender 可改善模块造型，不能把整城烘焙为不可编辑的替代模型。
- 城堡下方台面建筑、广场和雕塑：Blender 里按原资产建立副本，改善台基衔接、尺度、造型与细节。
- 地形：Blender 多轮修改山峡轮廓、坡面和临水关系。保护道路、城门、建筑基础和碰撞锚点，不靠移动建筑掩盖地形问题。

## 至少三轮，按上一轮对照决定具体修改

| 轮次 | 修改重点 | 对照检查 |
|---|---|---|
| 基线 | 当前原游戏几何进入 Blender，原机位及目标构图机位各渲染一张 | 标出模型实际差异，不将已有蓝夜实验当成已接回成果 |
| 1 | 地形大轮廓、广场台面、街道与塔基衔接 | 山峡高度层次、建筑与山体比例、可见进攻通道、临水关系 |
| 2 | 台面建筑、雕塑、城门外围细节 | 建筑前后层次、雕塑比例与朝向、边缘倒角、轮廓与通路宽度 |
| 3 | 材质与灯光统一 | 青蓝山峡、白墙蓝顶、暖窗灯火；同机位的冷暖面积和阴影可读性 |

每轮保存独立 .blend、渲染图、差异说明及下一轮动作。必须实际查看渲染与目标图；不以导出成功、网格数量或像素误差单项替代美术判断。生成目标图不是正交尺寸图，不能直接拿逐像素差异作为几何正确性依据。

每轮检查原路面高度、角色净空、模块接缝和WFC受保护空区。合入时8931原游戏直接加载优化成果，Godot同步版本；保留原作基线与回退。

## 当前状态

已有源与实验候选分散在 artifacts/citadel-blue-night/round-01..04、layering-01..02、shoreline-fix。必须先核对其来源和变换再选用；未确认其符合新目标，不直接发布。

本轮 Blender 原生 MCP 场景读取未返回；本地插件9876端口可连接，但尚未确认可执行命令。当前进程打开 bookshop.blend，未确认未保存状态，不覆盖文件，也不另开多份前台实例。需恢复响应后建立圣城副本与基线渲染。本文件是执行标准，三轮尚未完成。
