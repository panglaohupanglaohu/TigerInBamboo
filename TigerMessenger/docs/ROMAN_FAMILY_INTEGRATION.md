## 用户最新验收与纠正：保留整体，修头盔与左臂

用户已认可士兵整体造型，胸甲和裙甲保持当前方向。继续修目标图与现头盔的眉檐、护颊、鼻护和后颈贴合差异；另明确指出弓箭兵和长矛兵左前臂旋转反向，需用同视角目标图校正肩—肘—腕以及真实拇指/手掌朝向，不能仅把握持锚点重合当骨骼正确。弓兵还需核验靠颊拉弦与持弓臂伸展。

当前 review.html 已改为三兵种并排展示，含各自目标图、正侧背图和弓兵动作展开。当前候选还没有完成本轮 Web/Godot 回接。

## 最新独立检查：胸袖轮廓改善，裙腿尚须修正

`gladius-blue-chest-v2/report.json` 验证候选肩宽约 0.142、腰宽约 0.100，袖盖绑定原上臂节点；结构、握持通过。但增加真实三角表面交叉检测后，ready 姿势仍发现左腿/裙甲 16 对、右腿/裙甲 7 对相交。因此 `sampled_surface_crossings_clear=false`，全接触校验未通过，已要求在共享到六种之前修正局部净空。检测不覆盖共面重叠、完全包含或连续扫掠，不能代替全动作验收。

## 胸甲迭代实况

蓝短剑候选已从无袖菱形躯干改为宽肩收腰甲衣，补短袖肩盖和浅 V 颈口；第二轮又去掉水平桶状环带，改大块斜切分面。左肩袖缝仍在末次修补。对照入口：`artifacts/pipeline/roman-family-blender/review.html`。这是候选进度，不是游戏端交付。

独立静止姿态检查 `artifacts/pipeline/roman-family-blender-validation/gladius-blue-chest-v1/report.json`：55 个原节点及父级保留，Blend→GLB 结构回读通过；盾手/剑手实体表面最小间距约 0.002014 / 0.000873，盔缨实际薄轴与 Z 对齐。仅 ready 静止姿态，不代表动作中握持和全身穿插均通过。当前胸甲外观末修需重新验证文件版本。

## 当前优先修正：胸甲与参考图不一致（用户新增反馈）

已将目标图 `assets/concepts/roman-soldier-target-v2.png` 与 Blender 蓝短剑候选 `after-front.png` 实际对照：候选仍为无袖菱形躯干，上臂完全裸露，缺少目标图的短袖肩盖、宽肩收腰、领口与胸部大块分面。不能因盔缨和裙片改善就通过整体造型验收。

下一轮先修蓝短剑样本：重塑胸廓和肩线、补随上臂活动的蓝色短袖肩盖、整理领口与腰带衔接，再核验正侧背视角；通过后共享到长矛、弓箭与红蓝变体。原始躯干和节点归档保留；新副本仍待视觉与动态接触校验，尚未回接 Web / Godot。

## 最新 Blender 迭代发现：旧盔缨旋转规则需纠正

原 source n11 矩阵为单位矩阵，crest实体范围 X约±0.082、Z约±0.0067，原平面为XY；若以工厂武器明确+X前向，冠脊应沿前后X，旧适配额外+90Y会变横向。此前通过的是矩阵稳定/既定契约测试，不能证明这个方向契约本身正确。Blender候选必须按脸/武器前向与正侧顶视重新确定；不能机械复用Web临时压窄和旧90度规则。

# 罗马兵家族接入指南

本轮复用已认可的 Blender v3 头盔、十片裙甲和原红蓝盔缨，不重复生成三套模型。原工厂与归档保持原样；运行时按角色契约装配。

| 角色 | Web 新装配 | 动作处理 | 尚未完成 |
| --- | --- | --- | --- |
| 短剑红/蓝 | 盔甲、盔缨、裙甲、盾把 | 既有握剑/持盾与挥击适配 | 裙腿、攀爬全身穿插 |
| 长矛红/蓝 | 共用盔甲、盔缨、裙甲 | 保留原持矛、瞄准、投掷、重装、盾与双臂 | 新握持校正、盾身间距与全身穿插 |
| 长弓红/蓝 | 共用盔甲、盔缨、裙甲 | 保留原躯干、腿、双臂、弓片、弦、箭全部射击阶段 | 全身接触/穿插独立于动作一致性 |
| 木马绑缚、夜潜火把/盾矛 | 同样的共用盔甲 | 明确只装护具，不覆盖绑绳/火把动作 | 各专属交互全流程验收 |

`src/assets/romanSoldierEquipment.js` 管共用装配、身份/父级检查与回退；`src/world/romanSoldierCombatPose.js` 按武器区分表现权限；`src/world/saihojiPhalanx.js` 在真实生成、帧收尾、移除/重置调用。`src/world/romanWorldArmor.js` 在完整场景初始化后只绑定三个明确的静态原角色名称，由 main 每帧检查卸载释放。

调用 `bindRomanSoldierEquipment(actor)` 时弓/矛自动只装盔甲；特殊动作角色显式传 `{armorOnly:true}`。`actor.userData.setRomanEquipment(false)` 回退外观；不要通过显示已阵亡 actor 来展示新模型。外观回退不能重写弓矛当时的动态姿势，也不能恢复损坏盾牌。

完整世界验证：`rtk proxy node tools/pipeline/test_roman_family_world.mjs`。使用新浏览器上下文，先截原位置木马绑缚兵，再调用原 `debugSiege` 生成战场，检查三兵种实际实例装配。跳过运输与剧情，不计为从出生地完整游玩；不改用户当前标签页与存档。正式记录 `artifacts/pipeline/roman-family-world/report.json`。

短剑回归：`rtk proxy env ROMAN_TEST_BASE_URL=http://127.0.0.1:8765/TigerMessenger node tools/pipeline/test_roman_equipment_web.mjs`；原方阵：`rtk proxy node ../tools/test_phalanx.mjs`。家族独立动作/六色图由 `tools/pipeline/test_roman_family_web.mjs` 生成，正式入口 `artifacts/pipeline/roman-family-web/index.html`。

Godot 本轮没有自动同步这些 Web 装配。原兵种资产归档与 Godot 短剑战斗子集状态保留，不能把 Web 回接写成双端全家族验收。

## 弓兵专用护具避让

几何对照发现通用新版头盔/盔缨与原弓弦在 hold/loose 阶段新增穿插。修复采用兵种专用护具宽度：仅头盔上部横向 z×0.5，三片盔缨原局部 x×0.35（转正后为横向）；保留高度、朝向、父级及红蓝颜色。头盔与腰带铆钉虽同网格，但铆钉和裙带不缩放；使用独立共享几何副本，其他兵种不受影响。弓片/弦/箭和射击动作不移动。几何验收结果以家族 latest.json 所指正式报告为准。

第二轮连续180帧发现仅收窄头部不足：下弦仍与新增腰带/铆钉/裙甲交叉46帧，原版同姿势为0。历史失败证据保留在 bow-contact-2026-09-08T23-32-05.193Z。允许进一步按弓兵整套护具侧向净空适配，不能把“腰带不改”的临时方案当用户限制；射击动作与接触点继续保持。

**对照范围更正**：上述第二轮诊断漏含原裙甲，不能据“原0/新46”断言腰裙为新增。补齐原裙的公平对照报告 `bow-contact-2026-09-08T23-33-58.874Z/report.json` 显示原/候选均46帧下弦穿腰裙。最终保留头盔上部×0.5与盔缨侧宽×0.35；不继续压窄腰裙。新增头部穿弦已消除，原下弦穿裙仍是未修问题，不能宣称全套射击无穿插。
