# 苔庭松林与伏击（2026-09-10）

## 本批要求

保留25个原作松树seed与六景归属，以既有Blender候选替换Web旧球团松树。依据实际鲲脊背分散，不把树平铺到板外。传统蓝盔登陆后短暂整理，进入松冠下隐蔽；只有莫比斯舰队实际发现鲲，才展开伏击。重甲兵仍只能由舰队真实受击触发。

## 已接线与验证边界

- `src/assets/saihojiPineOptimized.js`：读取25个既有Blender GLB的同步几何副本，独立Web配色修订；源GLB与Blender未改。候选仍比认可图云冠薄、树皮简，不能称最终美术验收。
- `src/world/saihoji.js`：实际调用上述工厂；鲸背适配尺度由6改2.4，保留各树高低比例。原6倍松冠相对当前小鲸背过大；此值属于本次构图调整，不是资产源尺寸修改。
- `src/world/saihojiPineLayout.js`：在真实苔台上确定性选点，保留原seed与六景归属，树根朝鲸背上方，树根苔裙跟随。最近根距由0.67970增至1.26438米。整树分枝水平轮廓拓宽35%、高度增加20%以接近目标云冠，枝叶一起变换避免脱离；原GLB不变。移位后根苔统一合并，隔离苔庭远景draw calls由本轮中间版821降至500，三角形115834不变。
- 隐蔽槽必须真实地面支持、垂直射线命中相应树的叶冠，并离根及其他槽足够远；绑定island局部坐标随鲲运动，不绑定摇摆树冠。最终50槽，最低冠底离实际槽位≥1.10米，场景9项独立检查通过；正在复核实际50兵带装备蹲伏净空和行走，点位通过不等于全身通过。
- `src/world/saihojiAmbush.js` / `saihojiPhalanx.js`：新伏击状态接线；最终真实场景19项通过，源码稳定（artifacts/pipeline/saihoji-ambush-web/2026-09-09T20-52-56.438Z）。舰队提前到达也要等待50兵隐蔽才揭示鲲；等待10秒不射箭，实际scan/lift后触发伏击。50带装备蹲伏冠底最低余量0.05657米，up一致。测试明确摆放舰队，不称自然完整通关；赴隐蔽路径全身三角碰撞未验。

## 查看

- 单株目标色与模型对照：`artifacts/pipeline/saihoji-pines-web/index.html`
- 真实SceneModule近远景与布局检查：`artifacts/pipeline/saihoji-pine-layout/index.html`
- 真实网页：http://localhost:8931/TigerMessenger/

Godot尚未同步本批松树布局/配色/新伏击时序。此前Godot的25株导入状态不能代替本批同步。

## 用户再次指出造型不符：Blender v2（进行中）

用户2026-09-10要求找回原目标并质疑当前模型。目标仍为 `assets/concepts/ancient-pine-target-v1.png`，已直接展示，并置于布局验收页顶部。明确：本轮此前做的是已有v1 GLB网页接入和布局，不能称新一轮Blender建模完成。

根因已经在 `tools/pipeline/build_ancient_pine_candidate.py` 确认：树冠局部厚度被乘 `.57`，并减面至2050；木材平滑/减面削弱了目标的粗粝折面。下一步先做811英雄株v2，保留原37枝段与29冠中心，修厚实分层云冠、连续根干与贴地根盘，再真实GLB回读同机位比较。v2在通过造型对照前不覆盖25株运行版本，也不把只有一株候选说成批量完成。

### 用户最新美术纠正：树干老态主要来自面配色

不要把“苍老”误解成堆凹凸和尖刺。以灰褐底、深棕纵向面带、浅灰棱面形成连贯的低多边形色差；目标的不同面配色是主要表现手段。v2必须把此配色存入实际GLB的材质/顶点颜色，不能只靠渲染灯光或网页截图后处理。冠厚修正继续保留，但树干逐面色彩优先。渲染对照使用同光照；后续Web/Godot需验证导入颜色保留。

## 踏步石恢复散置禅意（同日最新）

用户指出原作石头是随机、疏密有别的点状分布，现在变成道路不符合意境。查明原因：`addPilgrimagePath` 对六景中轴按1.35步距插值铺石，入口额外七石连续排列。现已撤除此连续路径生成，保留26块确定性随机散置踏步石，各庭4–7块、空庭仅2块；尺寸/朝向变化，实际苔台落点，避松根、主石与彼此叠压。去掉入口石时保留原随机采样消耗，以免改变后续庭石与苔斑。

最新实际SceneModule10项通过，包括全部26踏步石贴地且可见、25松树来源/朝向/间距、50冠下净空槽；伏击时序未改。前后同机位页：`artifacts/pipeline/saihoji-stepping-stones/index.html`。这批未同步Godot。

### 本次截图要求的两轮（进行中，不能拿旧轮次抵数）

用户提供的否定基准与目标裁剪已保存在 `assets/concepts/pine-review-2026-09-10/`。截图1的石柱状木干是被否定版本；之后必须新增 `user-round-1` 与 `user-round-2`，逐轮实际Blender修改、GLB回读、同机位渲染。第一轮去物理噪动并修纵向色面；第二轮根据第一轮图继续修正分叉根盘、过球形云冠/枝干细分，明确两轮差异。不得将被否定基准计为本次第一轮，不得仅重新渲染同模型充数。

## 鲲背覆盖目标与研究纠正（最新）

用户要求利用鲲整个可用背部、扩大苔庭承托面积并疏散松群。内置image_gen先生成全背布局v1/v2，已保存在assets/concepts/kun-back-garden-layout-target-v1.png、v2.png及prompt.md。用户指出与真实西芳寺差异大，尤其流水瀑布；已承认生成前未调研，两图均不作施工依据。

新调研来源：西芳会/西芳寺介绍 https://intosaihoji.com/en/saihoji/keidai/ 明确上段枯山水、下段黄金池回游；枯瀑为石组象征三级瀑布。京都市文化财介绍 https://www.city.kyoto.lg.jp/bunshi/page/0000005626.html 支持黄金池/洪隐山石组关系。项目六景名属于游戏分区，不称真实寺院六景。后续先看官方实景，保留鲲背全面利用目标，以连绵苔地、静水、嵌入坡面的枯石与疏密松群重绘，禁止流水瀑布、奇幻石山、连续铺装路。
