import {chromium} from '../../../tools/shot/node_modules/playwright/index.mjs';import {mkdir,writeFile} from 'node:fs/promises';import assert from 'node:assert/strict';
const out=new URL('../../artifacts/pipeline/citadel-shore-planting/',import.meta.url);await mkdir(out,{recursive:true});const browser=await chromium.launch({channel:'chrome',headless:true,args:['--use-angle=metal']});const report={};try{for(const variant of ['before','after']){const p=await browser.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(String(e)));if(variant==='before')await p.route('**/src/world/citadel/shorePlanting.js*',r=>r.fulfill({contentType:'text/javascript',body:'export function buildCitadelShorePlanting(){}'}));await p.goto('http://localhost:8931/TigerMessenger/?autostart=1&citadelCommonFrame=1&citadelOldHarbor=1',{timeout:180000});await p.waitForFunction(()=>window.__tm?.scene?.getObjectByName('old-shore-blender-rock-support'),null,{timeout:180000});
const row=await p.evaluate(async()=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer');t.P.daySpeed=0;t.P.timeOfDay=.85;t.cameraRig.update=()=>{};await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));t.camera.position.copy(c.localToWorld(new T.Vector3(0,34,170)));t.camera.up.set(0,1,0).transformDirection(c.matrixWorld);t.camera.lookAt(c.localToWorld(new T.Vector3(-6,14,42)));t.camera.fov=52;t.camera.updateProjectionMatrix();t.renderer.render(t.scene,t.camera);const grove=c.getObjectByName('citadel-shore-cypress-groves');let meshes=0,triangles=0;grove?.traverse(o=>{if(o.isMesh){meshes++;triangles+=(o.geometry.index?.count||o.geometry.attributes.position.count)/3;}});return {planting:c.userData.shorePlanting||null,meshes,triangles,image:t.renderer.domElement.toDataURL('image/png')};});await writeFile(new URL(variant+'.png',out),Buffer.from(row.image.split(',')[1],'base64'));delete row.image;const detail=await p.evaluate(()=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer');t.camera.position.copy(c.localToWorld(new T.Vector3(-38,12,61)));t.camera.lookAt(c.localToWorld(new T.Vector3(-49,-1,35)));t.camera.fov=38;t.camera.updateProjectionMatrix();t.renderer.render(t.scene,t.camera);return t.renderer.domElement.toDataURL('image/png');});await writeFile(new URL(variant+'-detail.png',out),Buffer.from(detail.split(',')[1],'base64'));row.errors=errors;report[variant]=row;assert.equal(errors.length,0);await p.close();}
assert.ok(report.after.planting.placed.length>=5);assert.ok(report.after.planting.placed.every(p=>p.walkEdgeClearance>=1.85&&p.root[1]<p.surface[1]));assert.ok(report.after.meshes<=5);await writeFile(new URL('report.json',out),JSON.stringify(report,null,2));console.log(JSON.stringify(report.after));}finally{await browser.close();}
