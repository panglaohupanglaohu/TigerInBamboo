import {chromium} from '../../../tools/shot/node_modules/playwright/index.mjs';
import {mkdir,writeFile} from 'node:fs/promises';
const phase=process.argv.includes('--before')?'before':'after',out=new URL('../../artifacts/pipeline/citadel-canopy-ground/',import.meta.url);await mkdir(out,{recursive:true});
const b=await chromium.launch({channel:'chrome',headless:true,args:['--use-angle=metal']});
try{const p=await b.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{const raf=requestAnimationFrame.bind(window);window.requestAnimationFrame=cb=>cb.name==='animate'?0:raf(cb);});await p.goto('http://localhost:8931/TigerMessenger/?autostart=1',{timeout:180000});await p.waitForFunction(()=>window.__tm?.messenger,null,{timeout:180000});
const start=p.getByRole('button',{name:'开始送信',exact:true});if(await start.isVisible())await start.click();
const r=await p.evaluate(()=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer'),m=c.getObjectByName('highland-canopy-groves'),ground=c.getObjectByName('citadel-oskar-grid-mountain-surface');c.updateWorldMatrix(true,true);const up=new T.Vector3(0,1,0).transformDirection(ground.matrixWorld),matrix=new T.Matrix4(),pos=new T.Vector3(),q=new T.Quaternion(),s=new T.Vector3(),ray=new T.Raycaster();ray.layers.enableAll();const rows=[];m.geometry.computeBoundingBox();for(let i=0;i<m.count;i++){m.getMatrixAt(i,matrix);matrix.decompose(pos,q,s);const bottom=new T.Vector3(pos.x,pos.y+m.geometry.boundingBox.min.y*s.y,pos.z).applyMatrix4(m.matrixWorld);ray.set(bottom.clone().addScaledVector(up,500),up.clone().negate());ray.far=1000;const hit=ray.intersectObject(ground,false)[0];rows.push({i,gap:hit?hit.distance-500:null});}t.cameraRig.update=()=>{};t.P.daySpeed=0;t.P.timeOfDay=.85;t.camera.position.copy(c.localToWorld(new T.Vector3(-40,42,141)));t.camera.up.set(0,1,0).transformDirection(c.matrixWorld);t.camera.lookAt(c.localToWorld(new T.Vector3(-18,19,16)));t.camera.fov=52;t.camera.far=3000;t.camera.updateProjectionMatrix();t.renderer.render(t.scene,t.camera);const grove=c.getObjectByName('highland-mountain-slope-vegetation');const treeRows=grove.children.filter(o=>o.visible&&o.userData.role==='mountain-slope-vegetation').map(o=>{const root=o.getWorldPosition(new T.Vector3());ray.set(root.clone().addScaledVector(up,500),up.clone().negate());ray.far=1000;const h=ray.intersectObject(ground,false)[0];return {name:o.name,castlePosition:c.worldToLocal(root.clone()).toArray(),gap:h?h.distance-500:null};});return {trees:treeRows,treePlacement:grove.userData.groundPlacement??null,count:m.count,missing:rows.filter(r=>r.gap===null).length,floating:rows.filter(r=>r.gap>.08).length,buried:rows.filter(r=>r.gap<-.25).length,rows,placement:m.userData.groundPlacement??null};});await p.screenshot({path:new URL(phase+'.png',out).pathname});r.errors=errors;await writeFile(new URL(phase+'.json',out),JSON.stringify(r,null,2));console.log(JSON.stringify({...r,rows:undefined}));if(phase==='after'&&(r.missing||r.floating||r.buried||r.trees.some(t=>t.gap===null||t.gap>.08||t.gap<-.25)||errors.length))process.exitCode=1;
}finally{await b.close();}
