const {chromium}=require('../../../tools/e2e/node_modules/playwright-core');const fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true,args:['--use-angle=metal']});try{const p=await b.newPage();await p.goto('http://localhost:8931/TigerMessenger/?autostart=1');await p.waitForFunction(()=>window.__tm?.scene.getObjectByName('planet-v8-curved-ocean'),null,{timeout:180000});const d=await p.evaluate(()=>{const o=window.__tm.scene.getObjectByName('planet-v8-curved-ocean'),g=o.geometry,u=o.material.uniforms;o.updateWorldMatrix(true,false);return {source:o.name,matrix:o.matrixWorld.toArray(),positions:Array.from(g.attributes.position.array),normals:Array.from(g.attributes.normal.array),water0:Array.from(g.attributes.waterData0.array),water1:Array.from(g.attributes.waterData1.array),indices:Array.from(g.index.array),color:u.uColor.value.toArray(),opacity:u.uOpacity.value,official:!!o.userData.officialOcean}});fs.writeFileSync('TigerMessenger/godot/data/original-ocean.json',JSON.stringify(d));console.log({vertices:d.positions.length/3,triangles:d.indices.length/3,official:d.official});}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
