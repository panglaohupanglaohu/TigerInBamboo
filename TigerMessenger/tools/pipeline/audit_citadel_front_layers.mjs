import {chromium} from '../../../tools/shot/node_modules/playwright/index.mjs';import {writeFile,mkdir} from 'node:fs/promises';const out=new URL('../../artifacts/pipeline/citadel-front-coastal-slope/',import.meta.url);await mkdir(out,{recursive:true});const b=await chromium.launch({channel:'chrome',headless:true,args:['--use-angle=metal']});try{for(const phase of ['after']){const p=await b.newPage({viewport:{width:1200,height:800}});await p.addInitScript(()=>{const raf=requestAnimationFrame.bind(window);window.requestAnimationFrame=cb=>cb.name==='animate'?0:raf(cb);});if(phase==='before')await p.route('**/frontCoastalSlope.js*',r=>r.fulfill({contentType:'application/javascript',body:'export function shapeFrontCoastalSlope(){}'}));await p.goto('http://localhost:8931/TigerMessenger/?autostart=1',{timeout:180000});await p.waitForFunction(()=>window.__tm?.messenger,null,{timeout:180000});for(const [name,eye,look] of [['front-overview',[-35,55,170],[8,3,70]]]){const result=await p.evaluate(({eye,look})=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer');t.camera.position.copy(c.localToWorld(new T.Vector3(...eye)));t.camera.up.set(0,1,0).transformDirection(c.matrixWorld);t.camera.lookAt(c.localToWorld(new T.Vector3(...look)));t.camera.fov=55;t.camera.updateProjectionMatrix();t.renderer.render(t.scene,t.camera);const meshes=[];t.scene.traverseVisible(o=>{if(o.isMesh&&!o.userData.backlitHighlight&&!o.userData.isOutline)meshes.push(o)});const ray=new T.Raycaster();ray.layers.enableAll();const hits=[[620,550],[620,640],[600,700]].map(([x,y])=>{ray.setFromCamera(new T.Vector2(x/600-1,1-y/400),t.camera);return {x,y,hits:ray.intersectObjects(meshes,false).slice(0,5).map(h=>({name:h.object.name,parent:h.object.parent?.name,point:c.worldToLocal(h.point.clone()).toArray()}))}});return hits;},{eye,look});console.log(JSON.stringify(result));}await p.close();}}finally{await b.close();}
