import {chromium} from '../../../tools/shot/node_modules/playwright/index.mjs';import {mkdir,writeFile} from 'node:fs/promises';const out=new URL('../../artifacts/pipeline/citadel-front-strata/',import.meta.url);await mkdir(out,{recursive:true});const b=await chromium.launch({channel:'chrome',headless:true,args:['--use-angle=metal']});try{const p=await b.newPage();await p.addInitScript(()=>{const raf=requestAnimationFrame.bind(window);window.requestAnimationFrame=cb=>cb.name==='animate'?0:raf(cb);});await p.route('**/frontStrataBlender.js*',r=>r.fulfill({contentType:'application/javascript',body:'export function applyFrontStrataBlender(){}'}));await p.goto('http://localhost:8931/TigerMessenger/?autostart=1',{timeout:180000});await p.waitForFunction(()=>window.__tm?.messenger,null,{timeout:180000});const report=await p.evaluate(async()=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer'),city=c.getObjectByName('highland-west-city');const {officialOceanLevelAt}=await import('/TigerMessenger/src/world/waterV8/officialOcean.js');c.updateWorldMatrix(true,true);const meshes=[];for(const name of ['citadel-oskar-grid-mountain-surface','backlit-highlight-citadel-oskar-grid-mountain-surface']){const m=c.getObjectByName(name),g=m.geometry,p=g.attributes.position;meshes.push({name,positions:Array.from(p.array),indices:g.index?Array.from(g.index.array):null,colors:g.attributes.color?Array.from(g.attributes.color.array):null,toCity:city.matrixWorld.clone().invert().multiply(m.matrixWorld).elements,vertices:Array.from({length:p.count},(_,i)=>{const v=new T.Vector3().fromBufferAttribute(p,i),w=m.localToWorld(v.clone());return {i,author:city.worldToLocal(w.clone()).toArray(),altitude:w.length()-160-officialOceanLevelAt(w),bottom:g.attributes.shoreBoundaryBottom.getX(i)>.5};})});}return {meshes,source:'Actual 8931 after r12, western/front coast and final ocean perimeter; before new strata deltas'};});await writeFile(new URL('report.json',out),JSON.stringify(report,null,2));console.log(JSON.stringify(report.meshes.map(m=>({name:m.name,vertices:m.vertices.length}))));}finally{await b.close();}
