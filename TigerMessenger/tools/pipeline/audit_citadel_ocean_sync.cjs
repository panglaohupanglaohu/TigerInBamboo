const {chromium}=require('../../../tools/e2e/node_modules/playwright-core');const fs=require('fs');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true,args:['--use-angle=metal']});try{const p=await b.newPage();await p.goto('http://localhost:8931/TigerMessenger/?autostart=1');await p.waitForFunction(()=>window.__tm?.scene.getObjectByName('planet-v8-curved-ocean'),null,{timeout:180000});const d=await p.evaluate(()=>{const o=window.__tm.scene.getObjectByName('planet-v8-curved-ocean'),g=o.geometry,u=o.material.uniforms;o.updateWorldMatrix(true,false);return {source:o.name,matrix:o.matrixWorld.toArray(),positions:Array.from(g.attributes.position.array),normals:Array.from(g.attributes.normal.array),water0:Array.from(g.attributes.waterData0.array),water1:Array.from(g.attributes.waterData1.array),indices:Array.from(g.index.array),color:u.uColor.value.toArray(),opacity:u.uOpacity.value,official:!!o.userData.officialOcean}});const old=JSON.parse(fs.readFileSync('TigerMessenger/godot/data/original-ocean.json'));const comparison={};for(const key of ['positions','normals','water0','water1','indices','matrix','color']){let count=0,max=0;for(let i=0;i<Math.max(old[key].length,d[key].length);i++){const v=Math.abs((old[key][i]??Infinity)-(d[key][i]??-Infinity));if(v>1e-7){count++;max=Math.max(max,v);}}comparison[key]={count,max,oldLength:old[key].length,newLength:d[key].length};}fs.mkdirSync('TigerMessenger/artifacts/pipeline/citadel-ocean-sync',{recursive:true});fs.writeFileSync('TigerMessenger/artifacts/pipeline/citadel-ocean-sync/comparison.json',JSON.stringify(comparison,null,2));console.log(comparison);console.log({vertices:d.positions.length/3,triangles:d.indices.length/3,official:d.official});}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
