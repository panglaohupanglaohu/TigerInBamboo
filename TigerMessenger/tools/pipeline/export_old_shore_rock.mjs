import {chromium} from '../../../tools/shot/node_modules/playwright/index.mjs';import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch({channel:'chrome',headless:true,args:['--use-angle=metal']});try{const p=await browser.newPage();await p.goto('http://localhost:8931/TigerMessenger/?autostart=1&citadelCommonFrame=1&citadelOldHarbor=1',{timeout:180000});await p.waitForFunction(()=>window.__tm?.scene?.getObjectByName('citadel-old-shore-approach'),null,{timeout:180000});const data=await p.evaluate(async()=>{const t=window.__tm,T=t.THREE,c=t.scene.getObjectByName('castleContainer');const {createOceanHeightSampler}=await import('/TigerMessenger/src/world/citadel/oceanSurface.js');const sea=createOceanHeightSampler(c,160),control=c.userData.oldShoreApproach.control,rows=[];for(let k=1;k<control.length;k++){const a=new T.Vector3(...control[k-1]),b=new T.Vector3(...control[k]),d=b.clone().sub(a),n=new T.Vector3(d.z,0,-d.x).normalize(),count=Math.ceil(Math.hypot(d.x,d.z)/2);const strip=[];for(let i=0;i<=count;i++){const q=a.clone().lerp(b,i/count);strip.push([-6.5,-3.5,0,3.5,6.5].map((side,j)=>{const v=q.clone().addScaledVector(n,side);v.y=(j===0||j===4)?sea(v.x,v.z)-1.2:q.y-(j===2?.62:1.0);return v.toArray();}));}rows.push(strip);}return {control,rows};});await writeFile(new URL('../../artifacts/pipeline/citadel-old-harbor-grade/rock-source.json',import.meta.url),JSON.stringify(data));console.log('Exported six actual shore support strips');}finally{await browser.close();}
